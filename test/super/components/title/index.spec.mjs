import {
  expect
} from 'chai'

import Title from '#cogs/super/components/title'

describe('#cogs/super/components/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
