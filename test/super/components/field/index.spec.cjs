const {
  expect
} = require('chai')

const Field = require('#cogs/super/components/field')

describe('#cogs/super/components/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
