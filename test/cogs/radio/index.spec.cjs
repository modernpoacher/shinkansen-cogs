const {
  expect
} = require('chai')

const Radio = require('#cogs/cogs/radio')

describe('#cogs/cogs/radio', () => {
  describe('`Radio`', () => {
    it('is a function', () => {
      expect(Radio)
        .to.be.a('function')
    })
  })
})
