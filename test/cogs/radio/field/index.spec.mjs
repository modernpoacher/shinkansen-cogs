import {
  expect
} from 'chai'

import Field from '#cogs/cogs/radio/field'

describe('#cogs/cogs/radio/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
