const {
  expect
} = require('chai')

const Field = require('#cogs/cogs/radio/field')

describe('#cogs/cogs/radio/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
