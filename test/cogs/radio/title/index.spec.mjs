import {
  expect
} from 'chai'

import Title from '#cogs/cogs/radio/title'

describe('#cogs/cogs/radio/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
