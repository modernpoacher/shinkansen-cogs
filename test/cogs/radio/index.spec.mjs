import {
  expect
} from 'chai'

import Radio from '#cogs/cogs/radio'

describe('#cogs/cogs/radio', () => {
  describe('`Radio`', () => {
    it('is a function', () => {
      expect(Radio)
        .to.be.a('function')
    })
  })
})
