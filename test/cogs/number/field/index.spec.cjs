const {
  expect
} = require('chai')

const Field = require('#cogs/cogs/number/field')

describe('#cogs/cogs/number/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
