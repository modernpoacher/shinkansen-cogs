import {
  expect
} from 'chai'

import Number from '#cogs/cogs/number'

describe('#cogs/cogs/number', () => {
  describe('`Number`', () => {
    it('is a function', () => {
      expect(Number)
        .to.be.a('function')
    })
  })
})
