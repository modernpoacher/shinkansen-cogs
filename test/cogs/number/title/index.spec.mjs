import {
  expect
} from 'chai'

import Title from '#cogs/cogs/number/title'

describe('#cogs/cogs/number/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
