import {
  expect
} from 'chai'

import Field from '#cogs/cogs/password/field'

describe('#cogs/cogs/password/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
