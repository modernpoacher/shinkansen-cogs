import {
  expect
} from 'chai'

import Title from '#cogs/cogs/password/title'

describe('#cogs/cogs/password/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
