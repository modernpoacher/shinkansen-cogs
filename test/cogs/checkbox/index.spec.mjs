import {
  expect
} from 'chai'

import Checkbox from '#cogs/cogs/checkbox'

describe('#cogs/cogs/checkbox', () => {
  describe('`Checkbox`', () => {
    it('is a function', () => {
      expect(Checkbox)
        .to.be.a('function')
    })
  })
})
