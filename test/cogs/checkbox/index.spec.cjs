const {
  expect
} = require('chai')

const Checkbox = require('#cogs/cogs/checkbox')

describe('#cogs/cogs/checkbox', () => {
  describe('`Checkbox`', () => {
    it('is a function', () => {
      expect(Checkbox)
        .to.be.a('function')
    })
  })
})
