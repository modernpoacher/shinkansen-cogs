const {
  expect
} = require('chai')

const Title = require('#cogs/cogs/checkbox/title')

describe('#cogs/cogs/checkbox/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
