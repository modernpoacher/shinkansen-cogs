import {
  expect
} from 'chai'

import Title from '#cogs/cogs/checkbox/title'

describe('#cogs/cogs/checkbox/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
