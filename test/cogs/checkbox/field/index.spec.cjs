const {
  expect
} = require('chai')

const Field = require('#cogs/cogs/checkbox/field')

describe('#cogs/cogs/checkbox/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
