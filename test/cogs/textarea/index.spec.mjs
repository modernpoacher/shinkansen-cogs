import {
  expect
} from 'chai'

import Textarea from '#cogs/cogs/textarea'

describe('#cogs/cogs/textarea', () => {
  describe('`Textarea`', () => {
    it('is a function', () => {
      expect(Textarea)
        .to.be.a('function')
    })
  })
})
