import {
  expect
} from 'chai'

import Field from '#cogs/cogs/textarea/field'

describe('#cogs/cogs/textarea/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
