const {
  expect
} = require('chai')

const Field = require('#cogs/cogs/textarea/field')

describe('#cogs/cogs/textarea/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
