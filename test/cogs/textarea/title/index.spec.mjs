import {
  expect
} from 'chai'

import Title from '#cogs/cogs/textarea/title'

describe('#cogs/cogs/textarea/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
