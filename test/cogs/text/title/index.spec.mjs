import {
  expect
} from 'chai'

import Title from '#cogs/cogs/text/title'

describe('#cogs/cogs/text/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
