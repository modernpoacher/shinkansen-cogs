import {
  expect
} from 'chai'

import Field from '#cogs/cogs/text/field'

describe('#cogs/cogs/text/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
