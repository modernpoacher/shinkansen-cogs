import {
  expect
} from 'chai'

import Field from '#cogs/cogs/select/field'

describe('#cogs/cogs/select/field', () => {
  describe('`Field`', () => {
    it('is a function', () => {
      expect(Field)
        .to.be.a('function')
    })
  })
})
